# Commitメッセージ規約

## 基本フォーマット
```
<type>[(scope)]: <summary>

<body>

Refs: <ticket>
```
- 1行目は72文字以内。目的が即座に伝わる短い文にする。
- `type` は `feat` `fix` `docs` `refactor` `test` `chore` を使用。範囲を明示したい場合は `scope` を丸括弧で追加（例: `feat(raspberrypi)`）。
- `summary` は命令形で書き、末尾にピリオドを付けない。

## type の使い分け
- `feat`: 新機能やユーザーが認識できる振る舞いの追加。例: 新しいアニメーションモードや CLI オプション。
- `fix`: バグ修正や明確な欠陥の解消。例: SPI クロック設定ミスの修正。
- `docs`: README やコメントなどドキュメントのみの変更。
- `refactor`: 振る舞いを変えずに内部構造を整理する変更。例: 関数抽出、命名改善。
- `test`: テストの追加・修正のみを行う場合。
- `chore`: ビルドスクリプト、依存アップデート、CI 設定など運用上の雑務。

## 本文の書き方
- 本文が必要な場合はサマリ行と空行で区切る。
- 箇条書きで「何を」「なぜ」行ったかを中心に記述する。
- 影響範囲や注意事項があれば必ず記載する。
- 80文字程度で改行し、読みやすさを保つ。

## チケット・参照
- 課題番号や関連Issueがある場合は最後に `Refs: #123` のように追記。
- 外部トラッカーなら `Refs: PROJ-456` など、識別できる形式で書く。

## BREAKING CHANGES
- 互換性に影響する場合は本文末尾に `BREAKING CHANGE: <内容>` を追加し、利用者が必要なアクションを把握できるよう具体的に説明する。
